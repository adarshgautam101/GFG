class Solution {
    void swapKth(int arr[], int n, int k) {
        int j=arr.length-1;
        for(int i=0;i<arr.length;i++){
            if(k-1==i){
               int temp=arr[k-1];
               arr[k-1]=arr[j];
               arr[j]=temp;
            }
            j--;
        }
    }

}
